# Orchestrator P0

Runtime execution contracts for ready-work discovery, retry decisions, execution state, and execution events.

The core remains deterministic and side-effect free; transport, workers, persistence, leases, and durable scheduling stay behind explicit boundaries.
