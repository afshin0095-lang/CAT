[package]
name = "cat-prompt"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "CAT OMNISYSTEM provider-neutral prompt compilation and policy boundary"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
