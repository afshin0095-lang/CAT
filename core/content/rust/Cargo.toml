[package]
name = "cat-content"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "CAT canonical content-domain core"

[dependencies]
cat-eventbus = { path = "../../eventbus/rust" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
uuid = { version = "1", features = ["v7", "serde"] }
