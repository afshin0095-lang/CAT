[package]
name = "cat-kernel"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "CAT OMNISYSTEM deterministic kernel primitives"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
uuid = { version = "1", features = ["v7", "serde"] }
